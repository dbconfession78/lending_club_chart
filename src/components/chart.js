import React from 'react';
import {XYPlot} from 'react-vis';

const Chart = (props) => {
    return (
        <XYPlot
            width={1000}
            height={500}>
        </XYPlot>
    );
}

export default Chart;